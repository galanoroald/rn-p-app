import React from 'react';
import { StyleSheet, Text, Image, View} from 'react-native';
///Component

const GameView = (prop) => {

    return (
        <Text>{prop.name}</Text>
    );
}

export default GameView;
